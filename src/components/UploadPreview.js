

export default function UploadPreview(images) {

    return (
        // accept image array
        // convert each image to canvas
        // convert each canvas to json
        // pump that json into a new FrameList component
        <div>

        </div>
    );
}